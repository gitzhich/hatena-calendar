# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際の前提と規約を定める。

## プロジェクト概要

地下アイドル **XINXIN** の出演情報をカレンダー形式で表示する Web アプリ。
公式 X の投稿から出演情報（日時 / 会場 / イベント名 / 出演時間など）を集約し、
ファンが「いつ・どこで見られるか」を一目で把握できることを目的とする。

**ファンが個人で制作する非公式のツール**であり、XINXIN および運営とは関係がない。
一般公開し、ファンなら誰でも認証なしで閲覧できる形にする。
この位置づけは免責表示・削除要請対応の要件に直結する（`docs/requirements.md` LR-01 / LR-05）。

- まず Web アプリとして完成させる
- バックエンドが安定したら Expo (EAS) でスマホアプリを追加する
- **機能は少なめでよい。完成させることを最優先**にする

## 技術スタック

| 層 | 技術 | ホスティング |
| --- | --- | --- |
| フロントエンド / BFF | Next.js (App Router) / TypeScript | Vercel |
| バックエンド | Spring Boot / Java | Fly.io（**常時起動**） |
| DB | PostgreSQL | Neon（Free） |
| マイグレーション | Flyway（番号付き SQL。`backend/src/main/resources/db/migration/`） | — |
| 外部 API | X API v2（従量課金 / OAuth2 App-Only） | — |
| モバイル（将来） | Expo + EAS | EAS |

月額の見込みは約 $4〜7（X API 込み）。詳細は `docs/architecture.md`。

### 構成の原則

- **ブラウザから Spring Boot を直接呼ばない。** すべて Next.js を経由する（BFF 構成）。
  バックエンドの URL や内部 API キーを `NEXT_PUBLIC_` 付きの環境変数で扱わない
  （付けるとブラウザに露出する）
- **内部 API キーは公開用と管理用の 2 種類。** 管理キーは
  管理者セッションの検証に成功したときだけ使い、公開ページのレンダリングで読み込まない
- **Fly.io は常時起動、インスタンスは 1 台に固定。** スリープすると `@Scheduled` の
  取り込みが止まり、複数台だと多重起動する
- **Neon の無料枠はコンピュート時間（100 CU-hours/月）が制約。**
  公開ページは ISR でキャッシュし、閲覧アクセスを DB まで届かせない。
  これは性能だけでなく**可用性の防御**でもある
  （連打で無料枠を枯渇させられるとサイトが止まる。`docs/security.md` T-04）
- **ただし ISR だけでは足りない。** 年月ごとにキャッシュが分かれるため、
  カレンダーが受け付ける年月の範囲を有限にしないと、
  キャッシュミスを無制限に作られて DB に到達する（`docs/adr/0014-bounded-calendar-range.md`）
- **無料枠を最も食うのは閲覧者ではなく取り込みジョブ。** 実行のたびに
  Neon が最低 5 分起動するため、ポーリング間隔がそのまま消費量を決める
  （`docs/architecture.md` 第 11 章）

## ディレクトリ構成

モノレポ。1 リポジトリに以下を並べる。

```
.
├── CLAUDE.md
├── docs/          # 設計ドキュメント（後述）
├── frontend/      # Next.js
├── backend/       # Spring Boot
└── mobile/        # Expo（将来追加。今は作らない）
```

Web の API 契約をそのままモバイルから叩けるようにしておく。
**フロントエンド固有のロジックをバックエンドに埋め込まない**こと
（モバイル追加時に破綻するため）。

## docs/ の運用

設計は必ず `docs/` に文書として残してから実装する。実装だけ先行させない。

| ファイル | 内容 |
| --- | --- |
| `docs/requirements.md` | 要件・スコープ・非スコープ |
| `docs/architecture.md` | 全体構成、コンポーネント分割、デプロイ構成 |
| `docs/data-model.md` | エンティティとスキーマ |
| `docs/api.md` | REST API 契約（エンドポイント / リクエスト / レスポンス） |
| `docs/x-integration.md` | X API 連携の設計（取得・課金・パース） |
| `docs/security.md` | 脅威モデルと対策 |
| `docs/runbook-*.md` | 作業手順書（設計ではなく手順）。現状は `runbook-x-api-setup.md` のみ |
| `docs/adr/` | 意思決定記録。一覧と運用ルールは `docs/adr/README.md` |

ドキュメントと実装が食い違ったら、**ドキュメントを正として実装を直す**か、
先にドキュメントを更新してから実装する。放置しない。

## MVP スコープ

### 作るもの

- 出演情報のカレンダー表示（月表示）
- 出演情報の詳細表示（日時 / 会場 / イベント名 / 出演時間 / 物販時間 / チケットリンク / 出典 URL）
- X API からの差分取り込み（後述）
- 管理者による出演情報の登録・編集・削除（取り込み結果の補正を含む）
- 閲覧はログイン不要、管理操作のみ認証必須

前提:

- 情報源は **XINXIN 公式アカウント `@xinxin_official` 1 件のみ**。
  複数ソースの重複排除は考えない
- 管理者は **単一アカウント**（運用者本人のみ）。
  一般ユーザー登録も DB のユーザーテーブルも作らない

### 作らないもの（MVP 外）

- 一般ユーザーのアカウント登録、お気に入り、通知、リマインダー
- 複数アイドル / 複数グループ対応
- 週表示・日表示・リスト表示などの表示切り替え
- 過去投稿の完全なバックフィル（後述の 3,200 件上限のため best-effort に留める）

スコープを広げたくなったら、まず `docs/requirements.md` の非スコープに
入れて MVP を完成させる。

### 実装順序

1. 手動登録 + カレンダー表示だけで動く状態を作る
2. その上に X からの取り込みを載せる

取得層は**インターフェースで抽象化**し、手動登録のみでもアプリが成立する状態を保つ。
X API が落ちても止まらない構成にすること。

## X API 連携

### 料金体系（2026 年の改定を反映）

Free / Basic / Pro の月額プラン制は**廃止済み**。クレジットを事前購入し、
リクエストごとに消費する**従量課金（pay-per-usage）**が標準。

| 項目 | 単価 |
| --- | --- |
| Posts: Read | $0.005 / 1 リソース |
| User: Read | $0.010 / 1 リソース |
| 上限 | 月間 300 万 Post 読み取り（超過分は Enterprise 契約） |

課金仕様で特に重要な 2 点:

- **課金単位はレスポンスで返ってきたリソース数**。リクエスト数ではない
- 同一リソースは **24 時間（UTC）で重複排除**される。同じ投稿を同日中に何度取得しても
  課金は 1 回。ただしソフトな保証であり、障害時などに重複課金が起きうるとされている

Owned Read（$0.001）は自アプリのオーナー自身のデータのみが対象で、
他者の公式アカウントには適用されない。

**コスト感**: 1 アカウントが 1 日 10 投稿なら月 300 リソース ≒ **$1.5/月**。
`since_id` で差分だけ取れば実質この範囲に収まる。

### 使うエンドポイント

`GET /2/users/{id}/tweets`（Get User's Posts）。認証は Bearer Token（OAuth2 App-Only）。

主なパラメータ:

- `max_results`: 5〜100
- `since_id` / `until_id`、`start_time` / `end_time`
- `exclude`: `replies`, `retweets` — **出演情報だけなら両方除外する。そのまま課金削減になる**
- `tweet.fields`: `created_at,entities,attachments,public_metrics,note_tweet`
- `expansions=attachments.media_keys` + `media.fields=url,variants`（画像が必要な場合）
- ページング: `meta.next_token` → 次リクエストの `pagination_token`

ユーザー ID は `GET /2/users/by/username/{handle}` で**一度だけ**引き（$0.01）、
以降は DB に固定で保持する。毎回引かない。

### 設計上の必須ルール

- **取得済みの最大 tweet_id を永続化し、必ず `since_id` に渡す**。
  日をまたいで同じ投稿を取り直すと 24h の重複排除が効かず再課金されるため、
  「全件取り直し」の実装を絶対に書かない
- ポーリング間隔は 30 分。新規投稿がなければ返却リソースは 0 件＝課金ゼロだが、
  実行のたびに Neon が最低 5 分起動するため、間隔は X API ではなく
  **CU-hours の都合で決まっている**（`docs/architecture.md` 第 11 章）
- タイムラインは**遡れて約 3,200 件**という上限がある。投稿量が多いアカウントでは
  それより早く `next_token` が返らなくなる報告もあるため、
  初回の過去分取り込みは**取れるところまで**とし、失敗を前提に設計する
- 長文ポストは `note_tweet` に本文が入る。`text` だけ見ると途中で切れる
- 429（レート制限）は指数バックオフでリトライする。無限リトライにしない
- API 呼び出し数と取得リソース数をログ / メトリクスに残し、課金額を追跡できるようにする

### パース方針

投稿本文からの出演情報抽出は必ず失敗する前提で作る。

- パースできなかった投稿は破棄せず「未処理」として残し、管理画面から手動で登録できるようにする
- **抽出に成功した情報は承認を挟まずそのまま公開する**。告知の速報性を優先し、
  誤りは管理者の事後修正で直す。その代わり抽出精度を可能な限り高め、
  表記ゆれを網羅した抽出テストを用意する（`docs/requirements.md` FR-41）
- **自動取り込みが行うのは新規追加と、既存の出演情報の空欄補完のみ**。
  値が入っている列は上書きしない。公式は「公演情報解禁」と「タイムテーブル解禁」を
  分けて告知するため、後続の告知で出演時刻などの空欄が埋まる
  （`docs/data-model.md` 第 7.1 節）
- 日程変更・中止の告知は自動反映せず、管理者が手で直す
- 実際の告知投稿のサンプルが `docs/x-post-sample/` にある。
  パーサを書く前に必ず読むこと（絵文字が項目のマーカーになっている）。
  抽出ルールと検証済みの期待値は `docs/x-integration.md` 第 5 章

## セキュリティ方針

このプロジェクトはセキュリティを重視する。以下は実装時に必ず守る。
**なぜそれが必要か（脅威）と、漏洩・侵害が起きたときの手順は `docs/security.md`。**

### 認証・認可

- 管理系エンドポイントは認証必須。Spring Security で**デフォルト拒否**にし、
  公開エンドポイントだけを明示的に許可する（許可漏れではなく拒否漏れを防ぐ設計）
- 公開 API は読み取り専用。`GET` 以外を公開しない
- 認可チェックはコントローラ層だけでなくサービス層でも行う

### シークレット管理

- 認証情報 / API キー / DB パスワードをリポジトリにコミットしない
- **X の Bearer Token は課金に直結するシークレット**。環境変数で注入し、
  ログ・エラーレスポンス・フロントエンドに絶対に出さない
- `.env*` と `application-local.yml` は最初に `.gitignore` へ
- サンプルは `.env.example` として値を伏せて置く
- 誤ってコミットしたら、そのシークレットは**無効化・再発行する**（履歴からの削除だけで済ませない）

#### シークレットの値を観測しない

`.env` の中身を自分で確認しない。`cat` / `grep` での表示はもちろん、
**文字数・先頭文字・ハッシュなど値の性質を調べる操作も行わない**。
「設定できているか」を確かめる必要があるときは、**ユーザーに確認を依頼する**。

一方、`curl` などでシークレットを**使う**目的で環境に読み込むのは許容する。

```bash
set -a; . <(grep '^X_' ./.env); set +a   # 使うために載せる: 許容
echo "${#X_BEARER_TOKEN}"                 # 値を観測する: 禁止
```

**値を環境に載せることと、値を観測することは別**である。
前者は作業に必要だが、後者は必要になった時点でユーザーに聞けばよい。

### 入力・出力

- サーバ側検証を正とする（Bean Validation）。フロント側検証は UX のためのもの
- SQL は JPA / パラメータバインドのみ。文字列連結でクエリを組み立てない
- `dangerouslySetInnerHTML` を使わない。使う必要が出たら理由を docs に残し、必ずサニタイズする
- **X 由来のテキストは信頼しない入力**として扱う。URL・メンション・本文すべて検証・エスケープする

### 通信・ヘッダ

- CORS の許可オリジンは環境ごとに明示列挙する。`*` を使わない
- CSP / HSTS / `X-Content-Type-Options: nosniff` / `Referrer-Policy` を設定する
- 本番は HTTPS 必須

### その他

- 公開 API と管理ログインにレート制限をかける
- ログにトークン・パスワード・個人情報を出力しない
- 依存パッケージの脆弱性を定期チェックする（`npm audit` / OWASP Dependency-Check）
- エラーレスポンスでスタックトレースや内部構造を返さない

### 法務・コンプライアンス

- X の投稿本文をそのまま転載・保存しない。**日時・会場・イベント名といった事実データに
  正規化して保持**し、必ず出典 URL を添える
- 画像を無断で転載しない
- スクレイピングは X の利用規約違反かつアカウント停止リスクがあるため採用しない。
  取得は公式 API 経由のみ
- 一般公開するため、**全ページに非公式である旨を明示する**（`docs/requirements.md` LR-01）
- 削除要請を受け付ける連絡手段を用意し、
  **サイト全体を非公開にできる手段**を管理者が持つ（同 LR-05）
- **このリポジトリは外部公開しない。** `docs/x-post-sample/` に X 投稿の本文を
  そのまま置いており、公開すれば転載にあたる。抽出テストには実際の表記ゆれが要るため、
  架空データへの差し替えではなく非公開を選んでいる
  （`docs/adr/0015-private-repository.md`）。
  サンプルを追加するたび履歴に本文が積み上がるので、**この決定は実質的に不可逆**

## 未決定事項

実装を始める前に決着させる。決まったら本ファイルと `docs/adr/` を更新する。
構成に関する決定は `docs/architecture.md` にまとまっている。

1. **抽出精度の目標水準**。承認フローを持たないため、
   抽出精度が公開情報の品質を直接決める（`docs/requirements.md` 未決定事項 5）

各文書に固有の未決定事項は、それぞれの文書末尾を参照する。

## Git 運用

- **`main` ブランチに直接コミットしない**。変更は必ず作業ブランチを切って行い、
  `main` へはマージで取り込む
- 作業ブランチ名は種別を接頭辞にする（`feat/` `fix/` `docs/` `chore/`）
- **マージコミットを必ず残す**（PR なら `gh pr merge --merge`、ローカルなら `--no-ff`）。
  `--squash` と `--rebase` は使わない。
  どの作業がひとまとまりで入ったかを後から追えなくなるため
- **マージ済みの作業ブランチは削除する**（`git branch -d <branch>`）。手元に残さない

**取り込みは PR 経由で行う。** GitHub 側のブランチ保護（classic / ruleset）は
**private リポジトリだと GitHub Pro が必要**で、無料プランでは使えない。
代わりに `.githooks/` のローカルフックで止めている。

```bash
git config core.hooksPath .githooks   # clone 後に 1 回だけ実行する
```

- `pre-commit` … `main` 上での直接コミットを拒否する
- `pre-push` … `main` への直 push を拒否する

**`--no-verify` で迂回できるので保証ではない。** 事故を止めるためのもので、
規約そのものは人が守る。有料プランへ移るなら GitHub 側の保護に置き換える。

```bash
git checkout -b feat/xxx          # 作業ブランチを切る
git push -u origin feat/xxx       # push
gh pr create --fill               # PR を作る
                                  # 集約ジョブ ci の成功を確認してからマージする
gh pr merge --merge --delete-branch   # マージコミットを残して取り込む
git checkout main && git pull     # ローカルを追従させる
```

- **PR のマージは `--merge`**（マージコミットを残す）。`--squash` と `--rebase` は使わない。
  どの作業がひとまとまりで入ったかを追えなくなるため
- 必須チェックは集約ジョブ `ci` のみ。`backend` / `frontend` は
  変更のないディレクトリではスキップされ、チェックを報告しないため直接は指定できない

## 開発上の注意

### コマンド

ローカル DB は `compose.yaml`（PostgreSQL 17。Neon と同系）。
**`bootRun` の前に立ち上げておく。テストには要らない。**

```bash
docker compose up -d --wait          # PostgreSQL 17 を起動
docker compose down                  # 停止（データは volume に残る）

cd backend
./gradlew build                      # コンパイル + テスト
./gradlew test                       # テストのみ
./gradlew bootRun                    # 起動（:8080）

cd frontend
npm run dev                          # 開発サーバ（:3000）
npm run build                        # 本番ビルド
npm run lint                         # ESLint
```

Gradle ラッパーを同梱しているので、Gradle 本体の導入は不要（JDK 21 のみ要る）。
Flyway はバックエンドの起動時に自動で適用される。

**テストは `compose.yaml` の DB を使わない。** Testcontainers が使い捨ての
PostgreSQL を実行ごとに立てる（`PostgresContainerListener`）。
結合テストは各テーブルを削除するため、開発用の DB を共有していると
`./gradlew test` のたびに `source_account` まで消える。行が消えたことに
気づかず取り込みを動かすと、`last_fetched_tweet_id` がテストの残した値に
なっており、**取得範囲が意図せず広がって課金が跳ねる**。
Docker が動いていればテストは通る。

### 変異テスト

テストが本当に退行を捕まえるかは、**実装をわざと壊して確かめる**。
このリポジトリでは繰り返し穴が見つかっており、通っているテストが
規約を守っていない例が何度もあった。

```bash
scripts/mutation-test.py <<'EOF'
### desc: since_id を送らない
### file: backend/src/main/java/dev/mzhin/hatenacal/ingestion/XApiHttpClient.java
### tests: *XApiHttpClientTest*
--- from
case FetchWindow.Since since -> b.queryParam("since_id", since.sinceId());
--- to
case FetchWindow.Since since -> { }
EOF
```

`\u3000` や `\t` を含む行は、ヒアドキュメントを通る間に変質して
一致しなくなる。その場合は `--- from` の代わりに `### regex:` で
1 行を指す（リテラルを写す必要がなくなる）。

**手で `git checkout` して戻さない。** 修正が未コミットのまま実行すると、
変異ではなく修正ごと捨ててしまう。するとベースラインが壊れた状態で
テストが落ち、それが「変異を検出した」に見える。**偽陽性が黙って出る。**
スクリプトは作業ツリーが汚れていたら実行を拒否し、ベースラインが緑で
あることを確かめ、復元を git ではなく退避した中身から行う。
- タイムゾーンは **JST 固定**。ただし保存形式を 2 種類に分ける
  - **システムの時刻**（作成日時、取り込み日時など）は `TIMESTAMPTZ` で UTC 保存し、表示時に JST へ変換する
  - **イベントの開催日・開始時刻**は JST のローカル値として `DATE` / `TIME` で保存し、**UTC に変換しない**。
    暦日そのものが意味を持つ値であり、UTC 化するとカレンダーの月境界でズレるため
    （詳細は `docs/data-model.md`）
  - **深夜公演は時刻が実際に属する暦日に置く。**「26:00 開演」は開催日を翌日、
    出演開始時刻を `02:00` として保存する（`docs/adr/0011-midnight-date-rule.md`）。
    出演枠自体が日をまたぐ表記（「23:50-24:30」）は自動登録せず未処理に回す
  - イベント日時のズレは致命的なので、境界を曖昧にしない
- 日付境界のテストを必ず書く（月またぎ、深夜帯の出演、日をまたぐイベント）
- **X API を叩くテストを CI で実行しない**。実 API は課金されるため、
  テストはスタブ / 記録済みレスポンスに対して行う
