# AGENTS.md

**Cursor などのコーディングエージェント向けの入口。** Claude Code は `CLAUDE.md` を読む。

**このファイルに規約を書き写さない。** 正本は `CLAUDE.md` と `docs/` にある。
写すと必ず食い違い、どちらが正しいか分からなくなる。ここが持つのは
**正本への導線と、役割分担と、エージェント固有の禁止事項**だけ。

---

## 最初に読む

**自動では読み込まれない。作業を始める前に自分で開くこと。**

| ファイル | 内容 |
| --- | --- |
| `CLAUDE.md` | 前提・技術スタック・Git 運用・セキュリティ方針・X API 連携 |
| `docs/coding-guidelines.md` | 実装規約。日時の扱い、判定の置き場所、公開と管理の分離、表示とアクセシビリティ |

触る領域に応じて `docs/` の他の文書も読む。一覧は `CLAUDE.md`「docs/ の運用」にある。
`frontend/` を触るなら `frontend/AGENTS.md` も読む。

---

## 役割分担

**実装は Cursor、レビューは Claude Code。**

- Cursor は**作業ブランチを切って PR を出すところまで**を行う
- **マージとデプロイはしない。** `gh pr merge` と `scripts/deploy-backend.sh` を実行しない。
  デプロイは課金と可用性に直結し、取り消せない（[ADR-0019](docs/adr/0019-deploy-triggers-and-records.md)）
- **判断に迷った点を PR 本文に書く。** レビューはそこを起点にする。
  「なぜこうしたか」が書かれていない差分はレビューできない

---

## 実装せずに報告すること

次に当てはまると気づいたら、**手を止めて PR 本文かコメントで報告する。**
実装を進めない。

- **設計判断を伴う**（データ構造 / API 契約 / 命名規約 / ディレクトリ構成）
- **依存パッケージを足す必要がある。** 承認済みのものだけを使う（`frontend/AGENTS.md`）
- **`docs/` の記述と実装が食い違っている。** 文書が正で、実装を直すか、
  先に文書を更新する。放置しない（`CLAUDE.md`「docs/ の運用」）
- **`backend/` の変更が要る。** バックエンドは課金・認可・データ整合に直結する
- **課金に関わる値に触れる**（`since_id`、ポーリング間隔、取得範囲）

---

## 絶対にしないこと

- **`main` に直接コミット・push しない。** 作業ブランチは `main` の最新から切る。
  `.githooks` が止めるが、`--no-verify` で迂回しない
- **`--squash` / `--rebase` を使わない。** マージコミットを残す
- **`.env` の中身を読まない。** 表示はもちろん、文字数・先頭文字・ハッシュなど
  値の性質を調べる操作も行わない。設定の確認が要るときは人に依頼する
- **シークレットをコミットしない。** API キー・トークン・DB パスワード
- **`dangerouslySetInnerHTML` を使わない**
- **`NEXT_PUBLIC_` 付きの環境変数でバックエンド URL や API キーを扱わない。**
  ブラウザに露出する
- **`fly deploy` を直接叩かない。** `--ha=false` が落ちると予備機が立ち、
  取り込みが多重起動して課金が倍になる
- **X API を実際に叩くテストを書かない。** 実行のたびに課金される

---

## 検証

**変更した領域のものを必ず通してから PR を出す。**

```bash
cd frontend && npm run lint && npm run build && npm test
cd backend  && ./gradlew build          # Docker が要る（Testcontainers）
scripts/check-doc-refs.py               # 設計文書への参照が実在するか
```

`docs/` に触れたら `scripts/check-doc-refs.py` を必ず通す。
**設計文書への参照は章番号ではなく見出しテキストで書く**
（`docs/coding-guidelines.md`「設計文書への参照」）。番号は文書を並べ替えるだけでずれ、
しかも「番号としては解決するが別の章を指す」形で壊れる。
